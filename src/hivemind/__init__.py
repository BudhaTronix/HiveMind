"""HiveMind is an educational, local-first multi-agent research system.

The package keeps language-model calls, orchestration, persistence, and user-interface
code separate so a learner can inspect each concern without learning a framework first.
"""

__version__ = "0.1.0"
